services:
  parser-postgres:
    # image: postgres:16.3-alpine3.20
    build: ./infrastructure/twich_parser_service/compose/docker/postgres
    hostname: parser-postgres
    container_name: parser-postgres
    user: postgres
    entrypoint: ["/bin/postgres/entrypoint.sh"]
    command: ["postgres", "-c", "config_file=/etc/postgres/postgres.conf"]
    expose: ["${COMPOSE_TWT_PARSER_POSTGRES_PORT}/tcp"]
    env_file:
      - path: ./infrastructure/twich_parser_service/compose/env/postgres/.env
        required: true
    volumes:
      - type: bind
        source: ./infrastructure/twich_parser_service/compose/entrypoints/postgres
        target: /bin/postgres
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ./infrastructure/twich_parser_service/compose/configs/postgres
        target: /etc/templates/postgres
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ./infrastructure/twich_parser_service/compose/certs/postgres
        target: /etc/ssl/postgres
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ./infrastructure/common/compose/certs/ca/ca.crt
        target: /etc/ssl/ca/ca.crt
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: volume
        source: parser-postgres-data
        target: /storage/postgres
        volume:
          nocopy: false
      - type: volume
        source: parser-postgres-config
        target: /etc/postgres
        volume:
          nocopy: false
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${COMPOSE_TWT_PARSER_POSTGRES_USER} -d ${COMPOSE_TWT_PARSER_POSTGRES_DB}"]
      interval: 30s
      timeout: 30s
      retries: 10
      start_period: 30s
      start_interval: 10s
    logging:
      driver: json-file
      options:
        labels-regex: "^.+"
    deploy:
      resources:
        limits:
          cpus: ${COMPOSE_TWT_PARSER_POSTGRES_CPUS_LIMIT}
          memory: ${COMPOSE_TWT_PARSER_POSTGRES_MEMORY_LIMIT}
        reservations:
          cpus: ${COMPOSE_TWT_PARSER_POSTGRES_CPUS_RESERVATION}
          memory: ${COMPOSE_TWT_PARSER_POSTGRES_MEMORY_RESERVATION}
    networks:
      parser-postgres-network:
        ipv4_address: ${COMPOSE_TWT_PARSER_POSTGRES_IP_ADDRESS}
    pull_policy: missing
    restart: always

networks:
  parser-postgres-network:
    name: parser-postgres-network
    driver: bridge
    internal: false
    external: false
    attachable: false
    enable_ipv6: false
    ipam:
      driver: default
      config:
        - subnet: ${COMPOSE_TWT_PARSER_POSTGRES_NETWORK_SUBNET}
          ip_range: ${COMPOSE_TWT_PARSER_POSTGRES_NETWORK_IP_RANGE}
          gateway: ${COMPOSE_TWT_PARSER_POSTGRES_NETWORK_GATEWAY}

volumes:
  parser-postgres-data:
    name: parser-postgres-data
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "\
        size=${COMPOSE_TWT_PARSER_POSTGRES_DATA_SIZE},\
        uid=${COMPOSE_TWT_PARSER_POSTGRES_VOLUME_UID},\
        gid=${COMPOSE_TWT_PARSER_POSTGRES_VOLUME_GID}\
        "

  parser-postgres-config:
    name: parser-postgres-config
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "\
        size=${COMPOSE_TWT_PARSER_POSTGRES_CONFIG_SIZE},\
        uid=${COMPOSE_TWT_PARSER_POSTGRES_VOLUME_UID},\
        gid=${COMPOSE_TWT_PARSER_POSTGRES_VOLUME_GID}\
        "
