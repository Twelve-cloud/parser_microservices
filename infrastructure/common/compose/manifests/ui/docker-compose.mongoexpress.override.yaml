services:
  mongo-express:
    # image: mongo-express:1.0.2
    build: ./infrastructure/common/compose/docker/ui/mongoexpress
    hostname: mongo-express
    container_name: mongo-express
    user: node
    entrypoint: ["/bin/mongoexpress/entrypoint.sh"]
    command: ["node", "app"]
    ports:
      - name: mongo-express-port
        target: ${MONGO_EXPRESS_PORT}
        published: ${MONGO_EXPRESS_PUBLIC_PORT}
        host_ip: ${MONGO_EXPRESS_HOST_IP}
        protocol: tcp
        mode: host
    env_file:
      - path: ./infrastructure/common/compose/env/ui/mongoexpress/.env
        required: true
    volumes:
      - type: bind
        source: ./infrastructure/common/compose/entrypoints/ui/mongoexpress
        target: /bin/mongoexpress
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ./infrastructure/common/compose/configs/ui/mongoexpress
        target: /etc/templates/mongoexpress
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ./infrastructure/common/compose/certs/ui/mongoexpress
        target: /etc/ssl/mongoexpress
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ./infrastructure/common/compose/certs/ca/ca.pem
        target: /etc/ssl/ca/ca.pem
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: volume
        source: mongoexpress-data
        target: /opt/mongo-express
        volume:
          nocopy: false
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--spider",
          "--no-check-certificate",
          "https://${MONGO_EXPRESS_HOST}:${MONGO_EXPRESS_PORT}/health",
        ]
      interval: 30s
      timeout: 30s
      retries: 10
      start_period: 30s
      start_interval: 10s
    logging:
      driver: json-file
      options:
        labels-regex: "^.+"
    deploy:
      resources:
        limits:
          cpus: ${MONGO_EXPRESS_CPUS_LIMIT}
          memory: ${MONGO_EXPRESS_MEMORY_LIMIT}
        reservations:
          cpus: ${MONGO_EXPRESS_CPUS_RESERVATION}
          memory: ${MONGO_EXPRESS_MEMORY_RESERVATION}
    networks:
      mongo-express-network:
        ipv4_address: ${MONGO_EXPRESS_IP_ADDRESS}
      parser-mongo-network:
    depends_on:
      parser-mongo:
        restart: true
        condition: service_healthy
        required: true
    pull_policy: missing
    restart: always

networks:
  mongo-express-network:
    name: mongo-express-network
    driver: bridge
    internal: false
    external: false
    attachable: false
    enable_ipv6: false
    ipam:
      driver: default
      config:
        - subnet: ${MONGO_EXPRESS_NETWORK_SUBNET}
          ip_range: ${MONGO_EXPRESS_NETWORK_IP_RANGE}
          gateway: ${MONGO_EXPRESS_NETWORK_GATEWAY}
  parser-mongo-network:
    external: true

volumes:
  mongoexpress-data:
    name: mongoexpress-data
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: |
        size=${MONGOE_DATA_SIZE},uid=${MONGOE_VOLUME_UID},gid=${MONGOE_VOLUME_GID}
