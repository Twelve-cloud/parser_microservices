services:
  mongo-express:
    # image: mongo-express:1.0.2
    build:
      context: ${COMMON_MONGO_EXPRESS_CONTEXT}
      dockerfile: ${COMMON_MONGO_EXPRESS_DOCKERFILE}
    hostname: ${COMMON_MONGO_EXPRESS_HOST}
    container_name: mongo-express
    user: node
    entrypoint: ["${COMMON_MONGO_EXPRESS_ENTRYPOINT_TARGET}"]
    command: ["${COMMON_MONGO_EXPRESS_HOST}", "${COMMON_MONGO_EXPRESS_PORT}", "10"]
    ports:
      - name: mongo-express-port
        target: ${COMMON_MONGO_EXPRESS_PORT}
        published: ${COMMON_MONGO_EXPRESS_PUBLIC_PORT}
        host_ip: ${COMMON_MONGO_EXPRESS_HOST_IP}
        protocol: tcp
        mode: host
    env_file:
      - path: ${COMMON_MONGO_EXPRESS_ENV}
        required: true
    volumes:
      - type: bind
        source: ${COMMON_MONGO_EXPRESS_ENTRYPOINT_FOLDER_SOURCE}
        target: ${COMMON_MONGO_EXPRESS_ENTRYPOINT_FOLDER_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ${COMMON_MONGO_EXPRESS_TEMPLATE_FOLDER_SOURCE}
        target: ${COMMON_MONGO_EXPRESS_TEMPLATE_FOLDER_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ${COMMON_MONGO_EXPRESS_SSL_CRT_FOLDER_SOURCE}
        target: ${COMMON_MONGO_EXPRESS_SSL_CRT_FOLDER_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ${COMMON_MONGO_EXPRESS_SSL_CA_CRT_SOURCE}
        target: ${COMMON_MONGO_EXPRESS_SSL_CA_CRT_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: volume
        source: mongoexpress-data
        target: ${COMMON_MONGO_EXPRESS_STORAGE_FOLDER_TARGET}
        volume:
          nocopy: false
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "--silent",
          "--cacert",
          "${COMMON_MONGO_EXPRESS_SSL_CA_CRT_TARGET}",
          "https://${COMMON_MONGO_EXPRESS_HOST}:${COMMON_MONGO_EXPRESS_PORT}/health",
        ]
      interval: 30s
      timeout: 30s
      retries: 10
      start_period: 30s
      start_interval: 10s
    logging:
      driver: json-file
      options:
        labels-regex: "^.+"
    deploy:
      resources:
        limits:
          cpus: ${COMMON_MONGO_EXPRESS_CPUS_LIMIT}
          memory: ${COMMON_MONGO_EXPRESS_MEMORY_LIMIT}
        reservations:
          cpus: ${COMMON_MONGO_EXPRESS_CPUS_RESERVATION}
          memory: ${COMMON_MONGO_EXPRESS_MEMORY_RESERVATION}
    networks:
      mongo-express-network:
        ipv4_address: ${COMMON_MONGO_EXPRESS_IP_ADDRESS}
      parser-mongo-network:
    depends_on:
      parser-mongo:
        restart: true
        condition: service_healthy
        required: true
    pull_policy: missing
    restart: always

networks:
  mongo-express-network:
    name: mongo-express-network
    driver: bridge
    internal: false
    external: false
    attachable: false
    enable_ipv6: false
    ipam:
      driver: default
      config:
        - subnet: ${COMMON_MONGO_EXPRESS_NETWORK_SUBNET}
          ip_range: ${COMMON_MONGO_EXPRESS_NETWORK_IP_RANGE}
          gateway: ${COMMON_MONGO_EXPRESS_NETWORK_GATEWAY}
  parser-mongo-network:
    external: true

volumes:
  mongoexpress-data:
    name: mongoexpress-data
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "\
        size=${COMMON_MONGO_EXPRESS_DATA_SIZE},\
        uid=${COMMON_MONGO_EXPRESS_VOLUME_UID},\
        gid=${COMMON_MONGO_EXPRESS_VOLUME_GID}\
        "
