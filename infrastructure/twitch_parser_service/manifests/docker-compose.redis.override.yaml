services:
  parser-redis:
    # image: redis:7.4.0-alpine
    build:
      context: ${TWT_PARSER_REDIS_CONTEXT}
      dockerfile: ${TWT_PARSER_REDIS_DOCKERFILE}
    hostname: parser-redis
    container_name: parser-redis
    user: redis
    entrypoint: ["${TWT_PARSER_REDIS_ENTRYPOINT_TARGET}"]
    command:
      [
        "redis-server",
        "${TWT_PARSER_REDIS_CONFIG_TARGET}",
        "--user ${TWT_PARSER_REDIS_USERNAME} on >${TWT_PARSER_REDIS_PASSWORD} ~* &* +@all",
        "--user default off nopass -@all",
        "--requirepass ${TWT_PARSER_REDIS_PASSWORD}",
      ]
    expose: ["${TWT_PARSER_REDIS_PORT}/tcp"]
    env_file:
      - path: ${TWT_PARSER_REDIS_ENV}
        required: true
    volumes:
      - type: bind
        source: ${TWT_PARSER_REDIS_ENTRYPOINT_FOLDER_SOURCE}
        target: ${TWT_PARSER_REDIS_ENTRYPOINT_FOLDER_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ${TWT_PARSER_REDIS_TEMPLATE_FOLDER_SOURCE}
        target: ${TWT_PARSER_REDIS_TEMPLATE_FOLDER_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ${TWT_PARSER_REDIS_SSL_CRT_FOLDER_SOURCE}
        target: ${TWT_PARSER_REDIS_SSL_CRT_FOLDER_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ${TWT_PARSER_REDIS_SSL_CA_CRT_SOURCE}
        target: ${TWT_PARSER_REDIS_SSL_CA_CRT_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: volume
        source: parser-redis-data
        target: ${TWT_PARSER_REDIS_STORAGE_FOLDER_TARGET}
        volume:
          nocopy: false
      - type: volume
        source: parser-redis-config
        target: ${TWT_PARSER_REDIS_CONFIG_FOLDER_TARGET}
        volume:
          nocopy: false
    healthcheck:
      test:
        [
          "CMD",
          "redis-cli",
          "--no-auth-warning",
          "-h",
          "${TWT_PARSER_REDIS_HOST}",
          "-p",
          "${TWT_PARSER_REDIS_PORT}",
          "--tls",
          "--cert",
          "${TWT_PARSER_REDIS_SSL_CRT_TARGET}",
          "--key",
          "${TWT_PARSER_REDIS_SSL_KEY_TARGET}",
          "--cacert",
          "${TWT_PARSER_REDIS_SSL_CA_CRT_TARGET}",
          "--user",
          "${TWT_PARSER_REDIS_USERNAME}",
          "--pass",
          "${TWT_PARSER_REDIS_PASSWORD}",
          "ping",
        ]
      interval: 30s
      timeout: 30s
      retries: 10
      start_period: 30s
      start_interval: 10s
    logging:
      driver: json-file
      options:
        labels-regex: "^.+"
    deploy:
      resources:
        limits:
          cpus: ${TWT_PARSER_REDIS_CPUS_LIMIT}
          memory: ${TWT_PARSER_REDIS_MEMORY_LIMIT}
        reservations:
          cpus: ${TWT_PARSER_REDIS_CPUS_RESERVATION}
          memory: ${TWT_PARSER_REDIS_MEMORY_RESERVATION}
    networks:
      parser-redis-network:
        ipv4_address: ${TWT_PARSER_REDIS_IP_ADDRESS}
    pull_policy: missing
    restart: always

networks:
  parser-redis-network:
    name: parser-redis-network
    driver: bridge
    internal: false
    external: false
    attachable: false
    enable_ipv6: false
    ipam:
      driver: default
      config:
        - subnet: ${TWT_PARSER_REDIS_NETWORK_SUBNET}
          ip_range: ${TWT_PARSER_REDIS_NETWORK_IP_RANGE}
          gateway: ${TWT_PARSER_REDIS_NETWORK_GATEWAY}

volumes:
  parser-redis-data:
    name: parser-redis-data
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "\
        size=${TWT_PARSER_REDIS_DATA_SIZE},\
        uid=${TWT_PARSER_REDIS_VOLUME_UID},\
        gid=${TWT_PARSER_REDIS_VOLUME_GID}\
        "

  parser-redis-config:
    name: parser-redis-config
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "\
        size=${TWT_PARSER_REDIS_CONFIG_SIZE},\
        uid=${TWT_PARSER_REDIS_VOLUME_UID},\
        gid=${TWT_PARSER_REDIS_VOLUME_GID}\
        "
