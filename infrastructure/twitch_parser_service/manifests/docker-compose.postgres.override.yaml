services:
  parser-postgres:
    # image: postgres:16.3-alpine3.20
    build:
      context: ${TWT_PARSER_POSTGRES_CONTEXT}
      dockerfile: ${TWT_PARSER_POSTGRES_DOCKERFILE}
    hostname: ${TWT_PARSER_POSTGRES_HOST}
    container_name: parser-postgres
    user: postgres
    entrypoint: ["${TWT_PARSER_POSTGRES_ENTRYPOINT_TARGET}"]
    command: ["postgres", "-c", "config_file=${TWT_PARSER_POSTGRES_CONFIG_TARGET}"]
    expose: ["${TWT_PARSER_POSTGRES_PORT}/tcp"]
    env_file:
      - path: ${TWT_PARSER_POSTGRES_ENV}
        required: true
    volumes:
      - type: bind
        source: ${TWT_PARSER_POSTGRES_ENTRYPOINT_FOLDER_SOURCE}
        target: ${TWT_PARSER_POSTGRES_ENTRYPOINT_FOLDER_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ${TWT_PARSER_POSTGRES_TEMPLATE_FOLDER_SOURCE}
        target: ${TWT_PARSER_POSTGRES_TEMPLATE_FOLDER_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ${TWT_PARSER_POSTGRES_SSL_CRT_FOLDER_SOURCE}
        target: ${TWT_PARSER_POSTGRES_SSL_CRT_FOLDER_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: bind
        source: ${TWT_PARSER_POSTGRES_SSL_CA_CRT_SOURCE}
        target: ${TWT_PARSER_POSTGRES_SSL_CA_CRT_TARGET}
        read_only: true
        bind:
          propagation: rprivate
          create_host_path: true
          selinux: Z
      - type: volume
        source: parser-postgres-data
        target: ${TWT_PARSER_POSTGRES_STORAGE_FOLDER_TARGET}
        volume:
          nocopy: false
      - type: volume
        source: parser-postgres-config
        target: ${TWT_PARSER_POSTGRES_CONFIG_FOLDER_TARGET}
        volume:
          nocopy: false
    healthcheck:
      test: [
          "CMD",
          "psql",
          "
          host=${TWT_PARSER_POSTGRES_HOST}
          port=${TWT_PARSER_POSTGRES_PORT}
          user=${TWT_PARSER_POSTGRES_USER}
          password=${TWT_PARSER_POSTGRES_PASS}
          dbname=${TWT_PARSER_POSTGRES_DB}
          sslmode=verify-full
          sslcert=${TWT_PARSER_POSTGRES_SSL_CRT_TARGET}
          sslkey=${TWT_PARSER_POSTGRES_SSL_KEY_TARGET}
          sslrootcert=${TWT_PARSER_POSTGRES_SSL_CA_CRT_TARGET}
          ",
          "-c",
          "SELECT 'OK'",
        ]
      interval: 30s
      timeout: 30s
      retries: 10
      start_period: 30s
      start_interval: 10s
    logging:
      driver: json-file
      options:
        labels-regex: "^.+"
    deploy:
      resources:
        limits:
          cpus: ${TWT_PARSER_POSTGRES_CPUS_LIMIT}
          memory: ${TWT_PARSER_POSTGRES_MEMORY_LIMIT}
        reservations:
          cpus: ${TWT_PARSER_POSTGRES_CPUS_RESERVATION}
          memory: ${TWT_PARSER_POSTGRES_MEMORY_RESERVATION}
    networks:
      parser-postgres-network:
        ipv4_address: ${TWT_PARSER_POSTGRES_IP_ADDRESS}
    pull_policy: missing
    restart: always

networks:
  parser-postgres-network:
    name: parser-postgres-network
    driver: bridge
    internal: false
    external: false
    attachable: false
    enable_ipv6: false
    ipam:
      driver: default
      config:
        - subnet: ${TWT_PARSER_POSTGRES_NETWORK_SUBNET}
          ip_range: ${TWT_PARSER_POSTGRES_NETWORK_IP_RANGE}
          gateway: ${TWT_PARSER_POSTGRES_NETWORK_GATEWAY}

volumes:
  parser-postgres-data:
    name: parser-postgres-data
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "\
        size=${TWT_PARSER_POSTGRES_DATA_SIZE},\
        uid=${TWT_PARSER_POSTGRES_VOLUME_UID},\
        gid=${TWT_PARSER_POSTGRES_VOLUME_GID}\
        "

  parser-postgres-config:
    name: parser-postgres-config
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "\
        size=${TWT_PARSER_POSTGRES_CONFIG_SIZE},\
        uid=${TWT_PARSER_POSTGRES_VOLUME_UID},\
        gid=${TWT_PARSER_POSTGRES_VOLUME_GID}\
        "
