# TYPE                  DATABASE        USER            ADDRESS                        AUTH METHOD          AUTH OPTIONS

# only ssl              all db          all users       postgres-host                  hash on password     verify cert with ca
hostssl                 all             all             ${_POSTGRES_NETWORK}           scram-sha-256        clientcert=verify-ca

# unix socket (psql)    db              user                                           hash on password
local                   postgres        username                                       scram-sha-256
